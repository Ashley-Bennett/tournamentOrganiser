services:
  # Frontend — the whole app. Auth and all data go directly to Supabase.
  - type: web
    name: tournament-organizer-frontend
    runtime: static
    buildCommand: cd frontend && npm install && npm run build
    staticPublishPath: ./frontend/dist
