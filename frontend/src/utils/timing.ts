// Round and game timings.

/**
 * When timings started being captured.
 *
 * Nothing played before this release has a clock against it, so both the
 * organiser and the player empty states have to explain the gap — and they
 * have to name the same date. Kept here so the two cannot drift apart.
 */
export const TIMINGS_START_LABEL = "1 September 2026";
